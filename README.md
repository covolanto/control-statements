# control-statements
Control statements direct the flow of execution in your program. They let you decide which code runs, when it runs, and how many times.
This repository consist of :
1. Selection/Decision Statements
Make choices between different paths:

if - Executes if condition is true

if-else - Chooses between two paths

switch - Chooses from multiple options
